apiVersion: v1
kind: Service

metadata:
  name: booking-system-service
  namespace: booking-system

spec:
  selector:
    app: booking-system

  ports:
    - port: 8080
      targetPort: 8080

  type: NodePort